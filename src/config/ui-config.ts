export const UI_CONFIG = {
  /**
   * Whether UI components (Input, Textarea, Select, etc.) should hide their borders when disabled.
   */
  noBorderOnDisabled: false,

  /**
   * Maximum width for detail pages (ExamDetailPage, OrderDetailPage, etc.).
   * Set to undefined or null for no max width constraint.
   * Examples: "1400px", "1600px", "80rem", "90vw"
   */
  pageMaxWidth: "2000px",
  examPageMaxWidth: "2300px",
};
